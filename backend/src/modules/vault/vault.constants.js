/**
 * 🗄️ VAULT CONSTANTS
 * Central single source of truth for Vault sorting parameters and validation constraints.
 */

export const VAULT_ALLOWED_SORT_FIELDS = Object.freeze(['createdAt']);

export const DEFAULT_VAULT_SORT_BY = 'createdAt';

export const DEFAULT_VAULT_SORT_ORDER = 'desc';

export const VAULT_OCCASION_NAME_MAX_LENGTH = 200;
