Transactions, Virtualization, Infinite lists, Platform specific Options in Captions & Hashtags

//needed in the code
- delete refresh token from the db once revoked at the time of creation of new refresh token of that user
-because now category added by from modal if already category exist then close popup and show that already exist.because popup still open and in the background above the list showing error message.
